# --- API ---
# Base URL (no trailing slash); the client appends /all-clients-data
API_BASE_URL=https://api.gemalgo.com/api/alpaca-open
# Passed as the `api_code` query-string parameter
API_KEY=replace_me

# --- Output ---
OUTPUT_DIR=./out
